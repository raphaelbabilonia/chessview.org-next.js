# ChessView.org Next.js

Server-rendered public website for ChessView.org.

This app is the SEO-oriented public surface for ChessView. It consumes the existing Express API and renders public event pages as HTML with metadata, sitemap, robots rules, and JSON-LD event data.

## Open Source And Commercial Boundaries

- Source code is licensed under AGPL-3.0-only.
- The ChessView name, ChessView.org name, logos, icons, domains, and brand identity are reserved trademarks/brand assets. See [TRADEMARKS.md](./TRADEMARKS.md).
- Brand files under `public/brand/` and app icons derived from the ChessView logo are included for the official deployment and should be replaced in forks.
- Production databases, curated tournament/news data, source evidence, dedupe state, AI search indexes, analytics databases, embeddings, recommendations, uploads, and commercial datasets are not open source. See [DATA_LICENSE.md](./DATA_LICENSE.md).
- The official hosted ChessView service is governed by baseline [Terms](./TERMS.md) and [Privacy](./PRIVACY.md) policies, which should be reviewed by counsel before public commercial launch.
- Contributors must keep private data, third-party proprietary material, credentials, and production exports out of pull requests. See [CONTRIBUTING.md](./CONTRIBUTING.md).

See [OPEN_SOURCE_POLICY.md](./OPEN_SOURCE_POLICY.md) for the full policy.

## Why Next.js

The React/Vite frontend is a client-side app. That is fine for dashboards and authenticated workflows, but public pages benefit from server-rendered HTML so search engines and social crawlers can read event content immediately.

Initial public routes:

- `/`
- `/en`, `/es`, `/it`
- `/en/events`, `/es/events`, `/it/events`
- `/en/events/[id-or-slug]`, `/es/events/[id-or-slug]`, `/it/events/[id-or-slug]`
- `/en/countries/[country]`, `/es/countries/[country]`, `/it/countries/[country]`
- `/en/sources/[source]`, `/es/sources/[source]`, `/it/sources/[source]`
- `/sitemap.xml`
- `/robots.txt`

## Languages

The public site is localized in:

- English (`/en`)
- Spanish (`/es`)
- Italian (`/it`)

Localized URLs are used for SEO. The language selector also stores the visitor preference in `localStorage` under `chessview_locale` and mirrors it to a lightweight cookie for future server-side routing.

Human-facing routing is automatic:

- `/` redirects to the stored cookie language, or the browser `Accept-Language`, or English.
- `/events` and `/events/:id` redirect to the preferred localized route.
- The visible localized URL remains the canonical URL so search engines can index each language separately.

## Theme and Branding

The first brand direction is intentionally quiet and operational:

- Chess View wordmark with a compact `CV` mark.
- Light/dark themes with `chessview_theme` persisted in `localStorage`.
- Public pages use a restrained green/teal/accent palette to match tournament-management workflows.
- The current implementation uses plain CSS variables instead of Tailwind to stay close to the existing React frontend and keep the initial design system easy to audit.

## Slug Strategy

Event pages should use stable slugs generated by the backend.

- Canonical public event URL: `/:locale/events/:slug`.
- ID-based URLs still work for compatibility, but redirect to the slug when the API returns one.
- Sitemap entries use slugs when available.
- Country and source pages use normalized slugs derived from public event metadata.
- The sitemap is dynamic so newly imported scraper events can appear without rebuilding the app.
- If an event title changes, avoid changing the slug unless the organizer explicitly wants a URL change.

## Local Setup

```bash
npm install
cp .env.example .env.local
npm run dev -- --hostname 127.0.0.1 --port 3001
```

The app expects the backend at `http://127.0.0.1:5000/api` by default.

In development, API fetches use `cache: "no-store"` so scraper imports appear immediately. In production, API fetches use Next.js revalidation so public pages can be cached while still refreshing as tournament data changes.

## Environment Variables

```env
API_BASE_URL=http://127.0.0.1:5000/api
NEXT_PUBLIC_SITE_URL=http://127.0.0.1:3001
NEXT_PUBLIC_TRACKING_ENABLED=false
NEXT_PUBLIC_TRACKING_API_URL=http://127.0.0.1:5000/api/tracking/events
```

`API_BASE_URL` is server-only. Do not put secrets in variables prefixed with `NEXT_PUBLIC_`; those are exposed to browsers.

`NEXT_PUBLIC_TRACKING_ENABLED` and `NEXT_PUBLIC_TRACKING_API_URL` are public browser settings. Keep tracking disabled locally unless you intentionally want to send development events to your local API.

## First-Party Tracking

The public site sends first-party analytics events directly to the ChessView API when `NEXT_PUBLIC_TRACKING_ENABLED=true`.

Captured signals include pageviews, event detail views, country/source page views, event and news outbound clicks, filter submissions, language changes, theme changes, referrer domain, UTM tags, browser/device class, and coarse country headers when the API host provides them. Raw search text is reduced to `search=used`, and visitor/session ids are generated in browser storage then hashed by the backend.

Hidden dashboard:

```text
/tracking/cv-tracking-4f7e9d2b-2026
```

The dashboard code should be changed with `TRACKING_DASHBOARD_CODE` before this becomes sensitive operational data.

## Production Deployment

The GitHub Actions workflow deploys the public website as a self-hosted Next.js standalone server on the VPS. It expects these repository secrets:

- `VPS_HOST`: the VPS host or IP address, for example `148.230.112.224`.
- `VPS_USERNAME`: a root or sudo-capable deploy user on the VPS.
- `VPS_SSH_KEY`: the private SSH key whose public key is authorized for `VPS_USERNAME`.

The workflow builds with:

```env
API_BASE_URL=https://api.chessview.org/api
NEXT_PUBLIC_SITE_URL=https://chessview.org
NEXT_PUBLIC_TRACKING_ENABLED=true
NEXT_PUBLIC_TRACKING_API_URL=https://api.chessview.org/api/tracking/events
```

It assumes CloudPanel has a Node.js site for `chessview.org` using site user `chessview`, with nginx proxying to port `3001`. If CloudPanel creates a different site user or path, update `SITE_USER` and `TARGET_DIR` in `.github/workflows/deploy-website.yml`.

## Security and Open Source Notes

- `.env.local` is ignored and must not be committed.
- No private API keys are required for this public app.
- Security headers are configured in `next.config.mjs`.
- The package is marked private to avoid accidental npm publishing, not to make the repository closed source.
- License: GNU Affero General Public License v3.0 only for source code.
- The source license does not grant rights to ChessView trademarks, official brand assets, production data, curated datasets, AI/analytics assets, private infrastructure, or hosted-service data.

## Admin Frontend Split

This Next.js repository is only for the public, SEO-oriented site. Authenticated organizer/admin workflows stay in the React frontend repository unless the project later decides to migrate them.

## Backend

Run the backend from `chessview.org-backend`:

```bash
npm install
cp .env.example .env
npm run dev
```

For scraper work, use MongoDB persistence with `MEMORY_STORE=false`. For local demos without MongoDB, set `MEMORY_STORE=true` in the backend `.env`.

The public app automatically exposes imported tournaments through localized event pages, country pages, source pages, JSON-LD, robots, and the dynamic sitemap.

## Scripts

```bash
npm run dev
npm run build
npm run start
npm run lint
npm run smoke
npm run verify
```

`npm run verify` runs lint, production build, smoke tests against the local backend, and a dependency audit.
